import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

// Uppdatera metadata för bättre SEO och mobilvisning
export const metadata: Metadata = {
  title: "Bygger & Renoverar Stockholm | Din totalentreprenör",
  description: "Professionell badrumsrenovering, köksrenovering och nybyggnation i Stockholm. Certifierade hantverkare för ditt drömhem.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="sv">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased overflow-x-hidden`}
      >
        {children}
      </body>
    </html>
  );
}